class Pressao:
    def bool podeEntregarDepois():
        return false
    end;

    defstatic void main(String[] args):
        write("Caros alunos de Compiladores");
        write("Este eh um codigo de exemplo em Pyscal!");
        write("");

        if(var1 > v@r8):
            write("Pode, mas nao tem ponto.");
            write(-10.82-10.a'" pontos");
            write("testing 123"
        end;
    end;
end.
